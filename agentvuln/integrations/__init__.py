"""Framework integrations for agentvuln.

Each submodule has a soft dependency — the framework SDK is only required
when you actually import it. Install the matching extra:

    pip install "agentvuln[langchain]"
    pip install "agentvuln[openai]"
"""
